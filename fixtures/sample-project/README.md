# Adaptive Latent Routing for Multi-Stage Retrieval

This sample project studies a retrieval system that routes each query through a latent controller before dense matching. The repository contains training, evaluation, and result export code so that texor can turn it into a paper draft.

